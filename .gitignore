# Training outputs / checkpoints (huge — never commit)
outputs/
*.safetensors
*.bin
*.pt
checkpoint-*/

# Python
__pycache__/
*.pyc
venv/
.venv/
env/

# HF / datasets cache
.cache/
wandb/

# Stale fake-data figure (plots.py fallback). The real figure report/results.png
# (from analyze.py) and report/index.html ARE committed for the README.
report/figures.png

# Local scratch — but DO commit results/ summaries and cohorts/
*.log
.DS_Store
