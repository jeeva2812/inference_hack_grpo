torch
transformers
trl
datasets
accelerate
vllm
# tokenizer backends — Qwen GRPO checkpoints save only the slow tokenizer; these
# are needed to reload it as a fast tokenizer at eval time (else ValueError).
tiktoken
sentencepiece
# analysis + tracking (lightweight, also installable on a laptop)
numpy
matplotlib
wandb
# optional: sentence-transformers for embedding-based signals (tier 2.5)
# sentence-transformers
